# 知炼 EchoMind · 产品说明计划书（初稿）

> 知乎黑客松「知识炼金场：学习工具与知识生产」赛道
> 版本 v0.1 ｜ 初审材料

---

## 一、项目概述

### 1.1 一句话定位

**知炼是知乎问题页内的一套原生「凝炼模式」：把冗长的回答自动炼成「看得懂、信得过、做得到」的干货，帮用户把「收藏了」变成「学会了」。**

### 1.2 背景与问题

知乎是中文互联网少有的深度经验库，但它正面临一个结构性困境——**内容的价值被自己的结构埋没**：

- **获取效率低**：一条五千字的回答，真正有用的干货往往不到两成，却埋在个人叙事、铺垫和车轱辘话里，用户要自己从长文里淘。
- **信任信号失灵**：学历标签造假、软广混入、抖机灵挤占高赞，"专业身份"不再是可信信号，用户不知道该信谁。
- **看完不落地**：用户收藏了大量回答，但不知道哪些可信、哪些适合自己、下一步该做什么。

与此同时，小红书凭借「真实、即用、一屏见结论」抢走了"真实经验"的即时获取场景。知乎的深度内容仍在，却输在了**获取效率和可信度**上。

**一个关键数据佐证了方向**：小红书 800–1200 字的长文案，比 300 字以下的内容多拿 78% 的收藏——**用户从不排斥长内容，排斥的是"长而没料"**。知乎的问题不是"太长"，是"没被炼成料"。

### 1.3 产品主张

**知炼帮知乎把「高质量内容消费」升级成「可验证的学习转化」。**

---

## 二、核心创作思路

### 2.1 核心判断：不是「总结器」，是「炼金术」

市面上的 AI 总结器只做一件事——压缩文本。但压缩解决不了知乎的真问题：用户不是"读不完"，而是"读完不知道哪些可信、哪些能做、下一步干什么"。

知炼的思路是 **「先诊断、再提炼、保可追溯」** 三步：

1. **诊断**：判断这条回答是"怎么做 / 怎么想 / 别踩坑"三类中的哪一类，以及"值不值得凝炼"。
2. **提炼**：按类型分发不同模板，把长文炼成结构化干货。
3. **可追溯**：区分"AI 提炼"和"原文引用"，每条结论都能跳回原文核对。

### 2.2 三个关键设计决策

**决策一：按类型分发，不做「一套模板打天下」**

知乎回答至少有三种性质，硬套同一模板会产生"给行业观点帖也配 4 步行动清单"的尴尬：

| 类型 | 用户意图 | 凝炼模板 | 典型问题 |
|---|---|---|---|
| 怎么做型 | 教我照着做 | 结论 + 经验结构化 + 要点 | 如何转行 AI PM |
| 怎么想型 | 帮我下判断 | 核心观点 + 支撑理由 + 立场对照 | AI 会不会取代 PM |
| 别踩坑型 | 让我别犯错 | 结论 + 要点 | 转行踩过哪些坑 |

**核心规则：只有"可执行性高"才出要点**，观点帖、踩坑帖绝不硬套行动清单。

**决策二：三态默认分发，凝炼要克制**

凝炼不是无差别地把所有回答都炼一遍。根据「价值 × 长度」自动分发，**克制本身就是智能**：

| 状态 | 判定 | 呈现 |
|---|---|---|
| 自动凝炼 | 高价值 + 冗长（≥800 字） | 默认凝炼态，附「展开原文」 |
| 保持原文 | 高价值 + 简洁 | 原文，不折叠也不凝炼 |
| 折叠 | 低价值（软广 / 抖机灵） | 折叠成一行，可展开 |

把"用户主动开"变成"系统默认炼、用户可切回"，让凝炼成为零负担的省时工具。

**决策三：忠实性标注，和 AI 总结器划清界限**

AI 总结器最大的信任危机是"不知道哪句是编的"。知炼把每条凝炼结果明确标为两类：

- 🟡 **AI 提炼**——模型的归纳（一句话结论、经验结构化），可能有偏差，需谨慎。
- 🟢 **原文**——要点（答主原话的提炼），每条带「原文 ↗」跳回原文并高亮核对。

**AI 只负责归纳结构，可执行的东西全引原文、可追溯**——这是知炼与"AI 总结器"的根本区别，也是评委最可能 challenge 之处，我们主动作答。

---

## 三、产品方案

### 3.1 核心功能

1. **干货率预评分 + 时间成本**：每条回答标"干货率 24% · 读 5 分钟 · 凝炼 40 秒"，阅读前就知道值不值得读。
2. **类型识别**：自动判断回答是"怎么做 / 怎么想 / 别踩坑"。
3. **凝炼视图**：一句话结论 + 经验结构化（背景 / 做法 / 结果 / 风险）+ 要点（每条带「原文 ↗」跳转）。
4. **内容要点进度条**：原文模式下悬浮置顶，节点用 LLM 生成的 2-4 字短标签，点节点跳转到关键句并高亮加粗。
5. **评论反例显性化**：把评论区里的质疑、翻车、过时提醒提上来，作为"适用边界雷达"。
6. **可信度分层**：亲历者 > 从业者，徽章强度体现。
7. **适合谁**：每条凝炼结果底部一句话适用人群。

### 3.2 数据输入（知乎原生字段）

知炼的核心输入是知乎独有的结构字段，而非通用文本：

| 字段 | 用途 |
|---|---|
| 作者标签 / 身份 | 可信度分层（亲历者 / 从业者） |
| 赞同数 | 高赞质量信号 |
| 评论摘要 | 反例提醒、质疑、适用边界 |
| 多回答结构 | 跨回答共识与冲突 |

---

## 四、整体技术实现方案

### 4.1 架构总览

```
┌─────────────────────────────────────────────┐
│  前端（Vite + Vue）                          │
│  模拟知乎问题页（原生 App 框架）+ 凝炼视图      │
└──────────────────┬──────────────────────────┘
                   │ HTTP
┌──────────────────▼──────────────────────────┐
│  后端（FastAPI）                             │
│  问题 → 回答 → 凝炼 的 Agent 编排管线         │
│                                              │
│  ContentTypeDetector   三路融合类型识别       │
│  DensityScorer         干货率 / 价值 / 长度   │
│  Distiller             凝炼（结论/结构/步骤） │
│  CredibilityTagger     可信度分层            │
│  CommentSynthesizer    评论反例              │
│  Review                忠实性标注 + 锚点      │
│  Evaluator             忠实性护栏 + Judge    │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│  数据层（zhihu-cli 真实搜索，带缓存/限流/熔断）│
│  问题 / 回答 / 作者标签 / 赞同数 / 评论摘要    │
└─────────────────────────────────────────────┘
```

### 4.2 Agent 职责

| Agent | 职责 | 用户侧体现 |
|---|---|---|
| ContentTypeDetector | **三路融合**判断类型（LLM 语义 + Embedding 相似度 + Pattern 关键词） | 类型标签 |
| DensityScorer | 计算干货率、价值、长度，定三态 | 干货率 + 自动凝炼判定 |
| Distiller | 生成结论、经验结构化、要点（步骤+避坑合并） | 凝炼视图 |
| CredibilityTagger | 亲历者 / 从业者分层 | 可信度徽章 |
| CommentSynthesizer | 提炼评论反例与边界 | 反例提醒 |
| Review | 绑定 source 锚点、标注 AI 提炼 vs 原文 | 忠实性标注 + 跳转 |
| Evaluator | 忠实性护栏 + LLM-as-Judge 评测 | 质量评分（trace） |

### 4.3 关键技术点

1. **三路融合类型识别**：LLM（语义 70%）+ Embedding（相似度 20%）+ Pattern（关键词 10%）加权投票，带失败回退、软广纠偏、阈值降级。`LLM_PROVIDER=mock` 时走纯规则（零依赖零延迟），`openai` 时真调模型（已接 DeepSeek），LLM 任何失败都静默回退 Pattern。
2. **LLM 超短标签**：进度条节点的 2-4 字标签由 LLM 语义生成（如「先做项目」「抓本质」），mock 时回退规则截断。
3. **source 锚点**：每条要点绑定原文 chunk 的 `source_id`，实现"点『原文 ↗』跳回原文并高亮"——可追溯的技术前提。
4. **工具熔断/缓存/限流 + 搜索回退**：`zhihu_cli` 封装 TTL 缓存、限流、熔断；`search zhihu` 限流时自动回退 `question recommend + question answers` 接口，保证搜索可用。
5. **LLM-as-Judge 评测**：规则版忠实性护栏（验证要点不越界）+ LLM 打分「忠实性/可执行性」，形成质量回归闭环。
6. **干货率计算规则**：动作句数 ÷ 有效句数，结合赞同数、评论反例加权，得出可解释的价值评分。

### 4.4 共识算法设计：先筛再聚（规模化解法）

一个真实问题下往往有几十到上百条回答，**全算不现实（成本爆炸 + 噪音淹没），不算又失去"共识"的意义**。核心解法是：**不对所有回答算共识，而是先筛出"有效样本"，只对高可信的少数算共识。**

**核心原则：共识不是"投票"，是"证据聚合"。** 知乎的共识不是"多数同意"，而是"高可信来源的一致"——1 个亲历者说的"别裸辞"，比 50 个路人的抖机灵更有分量。这与知乎"赞同数 + 专业身份"的精英逻辑天然一致。

**三层漏斗**（把 100 条压到可执行少数）：

```
100 条回答
   │ 第一层：价值粗筛（规则/轻模型，便宜）
   ▼
20 条有效样本
   │ 第二层：类型识别 + 凝炼（重模型，只对这 20 条做）
   ▼
20 条核心主张
   │ 第三层：语义聚类（embedding 相似度）
   ▼
共识 / 分歧 / 孤例
```

- **第一层（粗筛，便宜）**：用赞同数、作者标签、软广检测、长度、结构这些无需 LLM 的信号，刷掉软广、抖机灵、无干货。100 → 约 20。
- **第二层（精算，贵）**：只对有效样本做凝炼，提取"核心主张"，把大模型调用从 100 次压到 20 次。
- **第三层（聚类）**：用语义相似度把核心主张归成共识 / 分歧 / 孤例（共识不是字面相同，而是语义一致）。

**共识的置信度分四档**，不做"是/否"二元判断：

| 档位 | 定义 | 示例 |
|---|---|---|
| 强共识 | 多个亲历者 + 高赞一致 | "先做作品"（5 位亲历者，3 位高赞） |
| 弱共识 | 多人提到，但来源质量一般 | "多逛 GitHub"（4 位从业者） |
| 分歧 | 明确对立，各有背景 | "要不要先学算法"（3 人说不用 / 2 人说要看岗） |
| 孤例 | 仅 1 条来源 | "先去大厂做外包"（标注低置信） |

**分歧不是坏事**——"这里知乎专家有分歧"本身就是高价值信息，告诉用户这不是定论。分歧要显性化，并标注双方各自背景。

**成本控制与展示策略**：

- 超过阈值（如 50 条）只取价值 Top 20 算共识，其余折叠为"另有 N 条类似回答"。
- 共识卡只展示 2–3 条强共识 + 1–2 条分歧，每条带"来源数 + 可信度"；其余收进「查看全部观点」。
- 每个共识/分歧点可点击回溯到支持它的回答——延续"忠实性标注 + 可追溯"原则，**共识也不做黑盒**。

**边界兜底**：全低价值 → 不生成共识卡；观点极度分散 → 显示"暂无共识，观点分歧大"；回答 <5 条 → 直接全凝炼，跳过漏斗。

### 4.5 当前进展与演进

- **前端**：用户版原型已接真实 API（搜索框 → `/api/condense/live` → 动态渲染），保留手机原生 App 框架 + 三态自动分发 + 展开原文 + 要点进度条 + 返回提炼；评委版保留全量注解（trace）。
- **后端**：FastAPI Agent 管线已落地——三路融合类型识别（LLM 已接 DeepSeek）、三态分发、要点凝炼、共识合并、忠实性护栏、LLM-as-Judge 评测；`zhihu_cli` 封装缓存/限流/熔断，并支持 `search zhihu` 限流时回退 `question recommend + question answers` 接口；`/api/condense/live` 已能拉真实知乎数据跑通全链路。
- **凝炼产物**：从「步骤 + 避坑」简化为统一「要点」列表，每条带「原文 ↗」跳转（不展示原文引用，保持简洁）。
- **信息分层原则**：普通用户看干净的凝炼结果 + 可追溯的原文跳转；评委通过 trace 查证每个判断依据（三路得分、干货率、忠实性）。
- **下一步**：① 补 Embedding 路（轻量 n-gram 哈希）让三路真正并行；② OAuth 登录上线冲人气奖；③ 凝炼结果知识库；④ 监控闭环。

---

## 五、与知乎社区生态的契合度（重点）

### 5.1 知乎的困境，正是知炼的机会

知乎的核心资产是**深度、专业、多元的经验内容**，这也是它与小红书最大的差异化。但这份资产正被自己的结构消耗：

- 深度内容藏在五千字长文里，获取成本极高，用户转向"一屏见结论"的小红书；
- 专业身份被造假侵蚀，可信度信号失灵；
- 高赞被抖机灵和软广占据，优质内容的能见度下降。

**知乎不缺内容，缺的是把内容"炼"成可消费、可信赖形态的能力**——这正是知炼做的事。

### 5.2 知炼是「知乎专属」的，不是通用总结器

知炼的差异化根植于知乎独有的结构，换个平台就失效：

1. **用知乎独有的字段**：作者标签、赞同数、评论反例、多回答结构——这些是知炼判断"可信度"和"反例边界"的核心输入，是小红书、公众号等平台没有的。
2. **解决知乎独有的问题**：经验帖冗长、身份真假难辨、看完不落地——这三个痛点在知乎最典型。
3. **激活知乎独有的护城河**：把"深度专业"这个知乎最大的差异化，从"劣势"（难消费）重新变成"优势"（被炼成可执行的干货）。

### 5.3 对知乎生态的正向价值

1. **留住外流用户**：把"遇事不决小红书"里那些需要深度答案的用户拉回来——他们不是不需要知乎的深度，是消费不动。
2. **建立差异化心智**：小红书给"即时经验"，知乎给"炼过的深度经验"，形成"小红书快、知乎深"的清晰分工。
3. **反哺内容生产**：当"干货率高"成为可见信号，会引导创作者生产更结构化、更有料的内容，正向净化社区调性。

---

## 六、场景价值

### 6.1 目标用户与场景

**职业入门**是首个切入场景：大学生、转行者、应届生，面对一个陌生领域（如 AI 应用开发、产品经理、运营），手里收藏了大量知乎回答，却不知道**先学什么、哪些可信、下一步做什么**。

### 6.2 场景价值量化

| 维度 | 价值 |
|---|---|
| 时间价值 | 五千字长文 → 40 秒干货，把阅读成本降到 1/10 |
| 信任价值 | 可追溯的原文引用 + 可信度分层 + 评论反例，让用户敢信 |
| 行动价值 | 从"收藏了"到"学会了"，每条干货能落地、能执行 |

### 6.3 一个完整场景

用户搜索「如何从零转行做 AI 产品经理」，打开问题页：

1. 一条 900 字的冗长回答**已自动凝炼**——一句话结论 + 要点，40 秒看懂；
2. 每条要点带「原文 ↗」跳回原文高亮核对，确认没被 AI 编造；展开原文时顶部要点进度条（LLM 短标签）可跳关键句；核对后点底部悬浮「返回凝炼」一键回到结论，划到其它回答时按钮自动消失；
3. 底部"适合谁"提示"适合有产品经验、目标非算法岗的人"，避免盲目照搬；
4. 评论反例提醒"目标岗偏算法时这条不适用"，补上了答主没说的边界；
5. 两条简洁的高价值回答保持原文，一条软广回答自动折叠——用户全程零操作。

---

## 七、差异化

| 对比对象 | 对方 | 知炼 |
|---|---|---|
| AI 总结器 | 只压缩文本，无法判断可信、无法落地 | 先诊断类型、再凝炼，且每条可追溯原文 |
| 小红书 | 真实即用，但深度不足 | 守住知乎的深度，同时补上"即用" |
| 知识管理工具 | 用户自己导入、自己整理 | 在知乎阅读现场自动触发，零门槛 |

---

## 八、未来规划

- **G1**（已完成）：接入知乎开放接口（`search zhihu` + 限流回退 `question` 接口）+ 接入 LLM（DeepSeek）生成凝炼，保留 trace。
- **G2**：跨回答"共识经验合并"（按 4.4 的"先筛再聚"算法）——把上百条回答收敛为 2–3 条强共识 + 1–2 条分歧，每条带来源数与可信度，标"被 N 个高赞回答验证"。
- **G3**：知识地图——把一个问题下的多回答聚合成一张可导航的学习路径。
- **G4**：软广识别与排序降权——长期信任工程。
- **G5**：上线评审。

---

## 九、一句话总结

**小红书教会了市场「经验要真实、要即用」，知乎手里有更深的料，却被自己的结构埋住了。知炼要做的，是把这份更深的料，重新炼成可信任、可执行、可追溯的形状——这正是知乎生态最需要的能力。**
